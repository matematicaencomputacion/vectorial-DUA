## Why

La orientación del Advisor debe verse en Master después del render, nunca
mezclada con la búsqueda.

## What Changes

- `orientation.js` + sección «Para ubicarte» (`aria-live=polite`)
- Copy del plan; CTA que re-dispara búsqueda; panel dev con respuesta cruda
- Checklist + Playwright `orientation-01-ubicarte.png`
